<%= form_with(model: drive_datum, local: true) do |form| %>
  <% if drive_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(drive_datum.errors.count, "error") %> prohibited this drive_datum from being saved:</h2>

      <ul>
      <% drive_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :drive_datum_user_id %>
  </div>

  <div class="field">
    <%= form.label :stats %>
    <%= form.text_area :stats, id: :drive_datum_stats %>
  </div>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time, id: :drive_datum_start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.datetime_select :end_time, id: :drive_datum_end_time %>
  </div>

  <div class="field">
    <%= form.label :start_loc %>
    <%= form.text_area :start_loc, id: :drive_datum_start_loc %>
  </div>

  <div class="field">
    <%= form.label :end_loc %>
    <%= form.text_area :end_loc, id: :drive_datum_end_loc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
